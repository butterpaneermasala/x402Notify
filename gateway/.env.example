# Gateway configuration (example)

# RPC endpoint used to verify on-chain payments (required)
# e.g. Infura/Alchemy/QuickNode URL for the testnet or mainnet you're using
RPC_URL=https://sepolia.base.org

# Chain ID for the RPC above (required)
CHAIN_ID=84532

# Minimum confirmations required when verifying a payment tx on-chain
# Set to 0 or 1 for fast testing; increase in production for safety
MIN_CONFIRMATIONS=1

# Redis URL for idempotency and queue integration (optional for simple setups)
# e.g. redis://localhost:6379/0
REDIS_URL=redis://localhost:6379/0

# Telegram Bot (optional): if set, gateway will forward messages to Telegram
# WARNING: do NOT commit real tokens to the repo. Use environment variables
# or a secrets manager in production.
TELEGRAM_BOT_TOKEN=REPLACE_WITH_TELEGRAM_BOT_TOKEN

# Wallet to receive x402 payments (optional for demo; gateway may verify payments to any address)
GATEWAY_WALLET_ADDRESS=0xYourWalletAddress

# Fallback message price (in ETH) to advertise in 402 responses when applicable
MESSAGE_PRICE_ETH=0.0001

# Demo prefix (optional) — used only in demo mode / tests
DEMO_TX_PREFIX=demo_

# Server
PORT=3000

# Redis connection retry behavior
REDIS_CONNECT_RETRIES=5
REDIS_CONNECT_DELAY_MS=500