# Example env for the dev agent service

# Agent private key used by the dev service/worker to pay on behalf of users
# Use a faucet-funded test key for testnets. For production, store keys in a
# secrets manager and never commit private keys to source control.
AGENT_PRIVATE_KEY=REPLACE_WITH_AGENT_PRIVATE_KEY

# Gateway URL that the dev_service will call for /notify
GATEWAY_URL=http://localhost:3000

# RPC endpoint used by SDK/worker to send & confirm payments
RPC_URL=https://sepolia.base.org
CHAIN_ID=84532

# Redis (optional) — used for job queueing (RQ) and idempotency
REDIS_URL=redis://localhost:6379/0

# API key to protect dev_service endpoints (optional but recommended)
DEV_SERVICE_API_KEY=changeme

# Telegram bot token if dev_service should forward messages or notify admins
# Leave blank in repos; set in deployment environment instead.
TELEGRAM_BOT_TOKEN=

# Optional: change DB path for sqlite persistence
DB_PATH=./dev_service.db

# Worker settings (optional)
# Number of worker threads/processes to run when using RQ
WORKER_CONCURRENCY=1
